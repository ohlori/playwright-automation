{
  "scripts": {
    "pretest": "tsc --incremental -p ./tsconfig.json",
    "all": "npx playwright test",
    "daily" : "npx playwright test tests/get-shipping-ids.test.ts",
    "summary" : "npx playwright test tests/quick-summary.test.ts",
    "shipping" : "npm run pretest && npx playwright test tests/shipping-status.test.ts",
    "to-ship" : "npx playwright test tests/get-data.test.ts"
  },
  "devDependencies": {
    "@types/node": "^17.0.5",
    "playwright": "^1.17.1",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@playwright/test": "^1.17.1"
  }
}
